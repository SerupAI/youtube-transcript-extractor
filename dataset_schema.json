{
  "actorOutputSchemaVersion": 1,
  "title": "YouTube Transcript Results",
  "description": "Structured results from YouTube transcript extraction with success/failure indicators and metadata",
  "properties": {
    "transcripts": {
      "title": "Extracted Transcripts",
      "description": "Successfully extracted YouTube video transcripts with metadata",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "statistics": {
      "title": "Extraction Statistics", 
      "description": "Summary statistics of the extraction run",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=json&fields=success,video_url,transcript_length,includes_timestamps"
    },
    "failures": {
      "title": "Failed Extractions",
      "description": "Videos that failed to extract with error details",
      "template": "{{links.apiDefaultDatasetUrl}}/items?format=json&clean=false&fields=success,video_url,error,attempts&desc=false&limit=1000&offset=0&omit=success:true"
    }
  }
}