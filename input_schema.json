{
  "title": "YouTube Transcript Extractor Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "type": "array",
      "description": "List of YouTube video URLs to extract transcripts from",
      "prefill": [
        { "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ" }
      ],
      "editor": "requestListSources",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "title": "URL",
            "type": "string",
            "description": "YouTube video URL to extract transcript from",
            "pattern": "^https?://.*"
          }
        },
        "required": ["url"]
      }
    },
    "language": {
      "title": "Language Preference",
      "type": "string",
      "description": "Preferred subtitle language (will auto-detect if not available)",
      "editor": "select",
      "enum": ["auto", "en", "es", "fr", "de", "it", "pt", "ja", "ko", "ru", "nl"],
      "enumTitles": ["Auto-detect", "English", "Spanish", "French", "German", "Italian", "Portuguese", "Japanese", "Korean", "Russian", "Dutch"],
      "default": "auto"
    },
    "preferManual": {
      "title": "Prefer Manual Subtitles",
      "type": "boolean",
      "description": "Prefer manually created subtitles over auto-generated ones",
      "editor": "checkbox",
      "default": true
    },
    "outputFormat": {
      "title": "Output Format",
      "type": "string",
      "description": "Format for the transcript output",
      "editor": "select",
      "enum": ["clean", "srt", "vtt"],
      "enumTitles": ["Clean Text (recommended)", "SRT with Timestamps", "Raw VTT Format"],
      "default": "clean"
    },
    "proxy": {
      "title": "Proxy Server",
      "type": "string",
      "description": "Optional proxy server for requests (recommended: residential proxy). Format: http://proxy:port or socks5://proxy:port",
      "editor": "textfield",
      "example": "http://residential-proxy.com:8080"
    },
    "includeTimestamps": {
      "title": "Include Timestamps",
      "type": "boolean",
      "description": "Include timestamps in transcript output (premium feature)",
      "editor": "checkbox",
      "default": false
    },
    "maxRetries": {
      "title": "Max Retries",
      "type": "integer",
      "description": "Maximum number of retry attempts per video",
      "editor": "number",
      "default": 3,
      "minimum": 1,
      "maximum": 10
    }
  },
  "required": []
}